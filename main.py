#!/usr/bin/env python3
"""Legacy entry point (`python main.py <url>`). Prefer `uv run site-auditor` / the installed `site-auditor` command."""
from auditor.cli import main

if __name__ == "__main__":
    main()
